<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.transaction.dao.ClassDAO">

    <!-- 增加 -->
    <insert id="add">
        insert into c(cno)
        values(#{cno})
    </insert>

</mapper>